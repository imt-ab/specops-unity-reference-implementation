# LifetimeScopes

This folder contains VContainer `LifetimeScope` implementations for the composition root.
- Use `LifetimeScope` to register dependencies.
- Wires all concrete implementations to interfaces.

